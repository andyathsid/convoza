# Path is relative to scripts/ when running npm run search:sync.
BACKEND_SA=../backend/firebase-service-account.json
MEILI_URL=http://localhost:7700
# Use the same privileged development key as backend/.env.
MEILI_API_KEY=
